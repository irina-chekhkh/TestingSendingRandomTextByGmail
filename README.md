To start working with tests, you need to create a file named src/test/resources/email-data.properties.

It must contain the following properties:

email = ...
password = ...
receiver = ...

You also need to create a file named src/test/resources/wrong-data.properties.

It must contain the following properties (with invalid data):

email = ...
password = ...
